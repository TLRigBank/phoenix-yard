# Phoenix Yard agent instructions

Read `GROK.md` before writing code.

Build Phase 0 engine first, then wire `prototype/index.html` screens to live match output.

Never put R-codes or scores on the face. Never filter the catalog in the UI.
